#include <stdio.h>

int main(int argc, char const *argv[])
{
    int i=0,num,v[1000],lim;

    while ((scanf("%d",&num)) == 1 && num != 0)
    {
        v[i++] = num;
    }
    
    scanf("%d",&lim);

    

    return 0;
}
